#include "catch.hpp"
#include "die.h"
#include "roll.h"
#include "phase.h"
#include "come_out_phase.h"
#include "point_phase.h"

int main() 
{
	srand(time(0));
	return 0;
}

void Roll::roll_die() 
{
    die1.roll();
    die2.roll();
	int *roll = Roll;
	roll = throw_die;

}

class Shooter
{
	while(come_out_phase(get_outcome RollOutcome::natural) || (RollOutcome::craps) )
	{
		return value;
		cout << value;
		roll = throw_die;
	}

	while(point_phase(get_outcome RollOutcome::point) || (RollOutcome::seven_out) )
	{
		return value;
		cout << value;
		roll = throw_die;
	}
};